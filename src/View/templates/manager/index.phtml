<?php

include $this->templateDir . "/_header.phtml"; ?>
<div class="container main-content">
    <div class="card shadow-sm">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h3 class="mb-0">Список менеджеров</h3>
            <a href="/manager/add" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Добавить менеджера
            </a>
        </div>

        <div class="card-body">
            <?php
            if (empty($list)): ?>
                <div class="alert alert-info" role="alert">
                    Пользователи не найдены.
                </div>
            <?php else: ?>
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead class="table-light">
                        <tr>
                            <th scope="col">ID</th>
                            <th scope="col">Логин</th>
                            <th scope="col">ФИО</th>
                            <th scope="col">Статус</th>
                            <th scope="col">Дата создания</th>
                            <th scope="col" class="text-end">Действия</th>
                        </tr>
                        </thead>
                        <tbody>
                        <?php
                        foreach ($list as $user): ?>
                            <tr>
                                <td><?= htmlspecialchars((string)$user->id) ?></td>
                                <td><?= htmlspecialchars($user->username) ?></td>
                                <td><?= htmlspecialchars($user->fio) ?></td>
                                <td>
                                    <span class="badge bg-<?= $user->status == 1 ? 'success' : 'secondary' ?>">
                                        <?= $user->status == 1 ? 'Активен' : 'Заблокирован' ?>
                                    </span>
                                </td>
                                <td><?= $user->createdAt->format("d.m.Y H:i") ?></td>
                                <td class="text-end">
                                    <a href="/manager/edit?id=<?= $user->id ?>" class="btn btn-sm btn-outline-primary">
                                        <i class="bi bi-pencil-square"></i> Редактировать
                                    </a>
                                    <a href="/manager/delete?id=<?= $user->id ?>" class="btn btn-sm btn-outline-danger"
                                       onclick="return confirm('Вы уверены, что хотите удалить этого пользователя?');">
                                        <i class="bi bi-trash"></i> Удалить
                                    </a>
                                </td>
                            </tr>
                        <?php
                        endforeach; ?>
                        </tbody>
                    </table>
                </div>
            <?php
            endif; ?>
        </div>
    </div>
</div>

<?php
include $this->templateDir . "/_footer.phtml"; ?>
